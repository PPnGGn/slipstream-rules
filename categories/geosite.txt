# geosite categories kept in the trimmed geosite.dat.

# infra / never-proxy
private

# Russian services - direct
category-ru
category-gov-ru
vk
ok
yandex
mailru


# ad blocking (some panels route this to block)
category-ads-all

# Not used. These would only matter for a "force blocked domains through the
# VPN" rule; the client's RU-bypass (docs/stage-b.md) doesn't need them —
# blocked sites aren't in category-ru, so they already go through the tunnel.
# Uncomment + re-run the workflow only if such a rule is added.
# ru-blocked
# ru-blocked-community
# re-filter
# ru-whitelist
